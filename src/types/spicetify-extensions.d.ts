declare namespace Spicetify {
    namespace CosmosAsync {
        /**
         * Registers a proxy around CosmosAsync that prints function calls to the console.
         */
        function registerProxy(): void;
    }
}